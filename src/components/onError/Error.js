import imgError from './tenor.gif';

const Error = () => {
   return (
      <img src={imgError} alt="Error" style={{height: 260, objectFit: 'cover', margin: '0 auto'}} />
   )
}

export default Error;